import random, time
class SmoothieMakersGame:
    def __init__(self):
        self.money = 0
        self.smoothies_blended = 0
        self.blender_limit = 5
        self.cashier_boost = 1
        self.backpack_limit = 10
        self.items_in_backpack = 0
        self.item1_count = 0
        self.item2_count = 0
        self.item3_count = 0
        self.ingredients = {}

    def get_user_choice(self, prompt_message, options_dict):
        while True:
            print(prompt_message)
            for key, value in options_dict.items():
                print(f"{key}. {value}")      
            choice = input("Enter your choice: ")
            if choice in options_dict:
                return choice
            else:
                print("Invalid choice!")

    def starting_menu(self):
        print("Welcome to...")
        time.sleep(0.5)
        print("Smoothie Makers!")
        menu_options = {
            "1": "Make a smoothie",
            "2": "Collect ingredients",
            "3": "Show money",
            "4": "Shop",
            "5": "Ingredients info",
            "6": "Leave"
        }
        while True:
            choice = self.get_user_choice("\nWhat would you like to do?", menu_options)
            if choice == "1":
                self.make_smoothie()
            elif choice == "2":
                self.collect_ingredients()
            elif choice == "3":
                self.show_money()
            elif choice == "4":
                self.shop()
            elif choice == "5":
                self.ingredients_info()
            elif choice == "6":
                print("Thanks for playing!")
                print("Game created by Ethan Tan and Pan Chenxuan")
                return

    def ingredients_index(self):
        print("\nIngredients Index:")
        print("1. Milk - Basic ingredient")
        print("2. Banana - Basic ingredient")
        print("3. Strawberry - Basic ingredient")
        print("4. Blueberry - Advanced ingredient")
        print("5. Apple - Advanced ingredient")
        print("6. Pear - Advanced ingredient")
        print("7. Ice Cream - Rare ingredient")
        print("8. Coconut - Rare ingredient")
        print("9. Sugar - Rare ingredient")

    def ingredients_info(self):
        print("\nIngredients Info:")
        print("Milk: A basic ingredient that adds creaminess to your smoothie. Price/unit: $2")
        print("Banana: A basic ingredient that adds sweetness and thickness to your smoothie. Price/unit: $3")
        print("Strawberry: A basic ingredient that adds a fruity flavor to your smoothie. Price/unit: $4")
        print("Blueberry: An advanced ingredient that adds a sweet and tangy flavor to your smoothie. Price/unit: $8")
        print("Apple: An advanced ingredient that adds a crisp and refreshing flavor to your smoothie Price/unit: $6")
        print("Pear: An advanced ingredient that adds a sweet and juicy flavor to your smoothie. Price/unit: $7")
        print("Ice Cream: A rare ingredient that adds a rich and creamy flavor to your smoothie. Price/unit: $12")
        print("Coconut: A rare ingredient that adds a tropical flavor to your smoothie. Price/unit: $10")   
        print("Sugar: A rare ingredient that adds sweetness to your smoothie. Multiplier/unit added: 5% increase")#my cher explain   why never     
    def show_money(self):
        print(f"You have ${self.money}.")

    def display_smoothies_blended_count(self):
        print(f"You have blended {self.smoothies_blended} smoothies.")

    def add_ingredient(self, ingredient_name, amount=1):
        if self.items_in_backpack + amount > self.backpack_limit:
            print("Not enough space in backpack!")
            return False
        if ingredient_name not in self.ingredients:
          self.ingredients[ingredient_name] = 0   
        self.ingredients[ingredient_name] += amount
        self.items_in_backpack += amount
        print(f"You got {amount} {ingredient_name}!")
        print(f"You have {self.items_in_backpack} items in your backpack.")
        return True

    def deduct_ingredients(self, ingredient_type, quantity, smoothie_ingredients):
        if ingredient_type not in self.ingredients or self.ingredients[ingredient_type] <= 0:
            print(f"You don't have any {ingredient_type}!")
            return

        if self.ingredients[ingredient_type] >= quantity:
            self.ingredients[ingredient_type] -= quantity
            smoothie_ingredients.extend([ingredient_type] * quantity)
            self.items_in_backpack -= quantity
            print(f"Used {quantity} of {ingredient_type}.")
        else:
            print(f"Insufficient {ingredient_type}! You only have {self.ingredients[ingredient_type]}.")
            
    def collect_ingredients(self):
        area_options = {
            "1": "Basic Area",
            "2": "Advanced Area",
            "3": "Beach Area",
            "4": "Go back to main menu"
        }
        while True:
            area_choice = self.get_user_choice("\nWhere would you like to collect ingredients?", area_options)
            if area_choice == "1":
                print("Heading to the basic area...\n")
                time.sleep(3)
                self.basic_area()
            elif area_choice == "2":
                print("Heading to the advanced area...\n")
                time.sleep(5)
                self.advanced_area()
            elif area_choice == "3":
                print("Heading to the beach area...\n")
                time.sleep(7)
                self.beach_area()
            elif area_choice == "4":
                return

    def basic_area(self):
        print("Welcome to the basic area!")
        print("You can collect milk, banana, and strawberry here.")
        area_options = {
            "1": "Scavenge for ingredients.",
            "2": "Go back to ingredient collection menu."
        }
        while True:
            scavenge_choice = self.get_user_choice("\nWhat would you like to do?", area_options)
            if scavenge_choice == "1":
                    print("Scavenging for ingredients...")
                    time.sleep(random.randint(2, 6))
                    found_ingredient = random.choice(["Milk", "Banana", "Strawberry"])
                    print(f"You found {found_ingredient}!")
                    self.add_ingredient(found_ingredient)
            elif scavenge_choice == "2":
                return

    def advanced_area(self):
        if self.smoothies_blended < 5:
            print("You need to blend at least 5 smoothies before accessing the advanced area!")
            return
        else:
            print("Welcome to the advanced area!")
            print("You can collect pear, apple, and blueberry here.")
            area_options = {
                "1": "Scavenge for ingredients.",
                "2": "Go back to ingredient collection menu."
            }
            while True:
                scavenge_choice = self.get_user_choice("\nWhat would you like to do?", area_options)
                if scavenge_choice == "1":
                        print("Scavenging for ingredients...")
                        time.sleep(random.randint(2, 6))
                        found_ingredient = random.choice(["Blueberry", "Apple", "Pear"])
                        self.add_ingredient(found_ingredient)
                elif scavenge_choice == "2":
                    return

    def beach_area(self):
        if self.smoothies_blended < 20:
            print("You need to blend at least 20 smoothies before accessing the beach area!")
            return
        else:
            print("Welcome to the beach area!")
            print("You can collect rare ingredients here.")
            area_options = {
                "1": "Scavenge for ingredients.",
                "2": "Go back to ingredient collection menu."
            }
            while True:
                scavenge_choice = self.get_user_choice("\nWhat would you like to do?", area_options)
                if scavenge_choice == "1":
                        print("Scavenging for ingredients...")
                        time.sleep(random.randint(2, 6))
                        found_ingredient = random.choice(["Ice Cream", "Coconut", "Sugar"])
                        self.add_ingredient(found_ingredient)
                elif scavenge_choice == "2":
                    return

    def make_smoothie(self):
        smoothie_ingredients = []
        ingredient_mapping = {
            "1": "Banana", "2": "Strawberry", "3": "Milk",
            "4": "Blueberry", "5": "Apple", "6": "Pear",
            "7": "Ice Cream", "8": "Coconut", "9": "Sugar"
        }
        smoothie_prices_mapping = {
            "Milk": 2, "Banana": 3, "Strawberry": 4, "Apple": 6,
            "Pear": 7, "Blueberry":8 , "Coconut": 10, "Ice Cream": 12, "Sugar":0
        }
        while True:
            print("\nAdd ingredients to your smoothie:")
            print("You have:")
            for item, count in self.ingredients.items():
                print(f"{item}: {count}")

            ingredient_choices = {k: v for k, v in ingredient_mapping.items()}
            ingredient_choices["10"] = "Finish making smoothie"

            choice = self.get_user_choice("\nWhich ingredient would you like to add?", ingredient_choices)

            if choice == "10":
                if len(smoothie_ingredients) >= 1:
                    print(f"Your smoothie contains: {', '.join(smoothie_ingredients)}")
                    print("Mixing...")
                    time.sleep(3)
                    smoothie_price=0
                    sugar_count=0
                    for i in smoothie_ingredients:
                        if i =="Sugar":
                            sugar_count+=1
                        else:
                            smoothie_price+=smoothie_prices_mapping[i]
                    sugar_multiplier=1+sugar_count*0.05
                    smoothie_price=smoothie_price*self.cashier_boost*sugar_multiplier
                    self.money += smoothie_price
                    self.smoothies_blended += 1
                    print(f"Congrats! You sold your smoothie for ${smoothie_price}!")
                    self.show_money()
                    self.display_smoothies_blended_count()
                else:
                    print("You do not have any ingredients added to your smoothie")
                break
            else:
                selected_ingredient = ingredient_mapping[choice]
                try:
                    quantity = int(input(f"How many {selected_ingredient} would you like to add? "))
                    if quantity <= 0:
                        print("Quantity must be positive.")
                        continue
                    if (len(smoothie_ingredients) + quantity) > self.blender_limit:
                        print(f"Adding {quantity} {selected_ingredient} would exceed the {self.blender_limit}-ingredient limit. You can only add {self.blender_limit - len(smoothie_ingredients)} more.")
                        continue
                    self.deduct_ingredients(selected_ingredient, quantity, smoothie_ingredients)
                except ValueError:
                    print("Invalid quantity. Please enter a number.")

    def shop(self):
        print("\nWelcome to the shop!")
        print("Here, you can buy upgrades for your blender and better cashiers!")
        time.sleep(0.5)

        while True:
            item1_cost = 100 + 150 * self.item1_count
            item2_cost = 300 + 450 * self.item2_count
            item3_cost = 100 + 50 * self.item3_count

            shop_options = {
                "1": f"Upgrade blender (Cost: ${item1_cost})",
                "2": f"Upgrade cashiers (Cost: ${item2_cost})",
                "3": f"Upgrade Backpack size (Cost: ${item3_cost})",
                "4": "Go back to main menu"
            }
            self.show_money()
            choice = self.get_user_choice("\nWhat would you like to buy?", shop_options)

            if choice == "1":
                if self.money >= item1_cost:
                    self.money -= item1_cost
                    self.blender_limit += 5
                    self.item1_count += 1
                    print("Blender upgraded! You can now add more ingredients to your smoothie!")
                    self.show_money()
                else:
                    print("You don't have enough money to buy this item.")
            elif choice == "2":
                if self.money >= item2_cost:
                    self.money -= item2_cost
                    self.item2_count += 1
                    self.cashier_boost += 0.2
                    self.cashier_upgraded = True
                    print("Cashiers upgraded! You can now sell smoothies at a higher price!")
                    self.show_money()
                else:
                    print("You don't have enough money to buy this item.")
            elif choice == "3":
                if self.money >= item3_cost:
                    self.money -= item3_cost
                    self.item3_count += 1
                    self.backpack_limit += 10
                    print("Backpack upgraded! You can now carry more ingredients!")
                    self.show_money()
                else:
                    print("You don't have enough money to buy this item.")
            elif choice == "4":
                print("Exiting shop...")
                time.sleep(2)
                return

game = SmoothieMakersGame()
game.starting_menu()
